[package]
name = "rx"
version = "0.1.0"
edition = "2024"

[lib]
name = "rx"
path = "src/lib.rs"

[dependencies]
zip = { version = "4", default-features = false, features = ["deflate-flate2", "deflate-flate2-zlib-rs"] }
serde = { version = "1.0", features = ["derive"] }